<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="{{ path('app_index') }}">
        <span class="nav-title d-flex align-items-center">
            <img src="{{ asset('build/images/logo.png') }}" class="logo-nav mr-4 ml-4 anim-logo" alt="logo de wild series">
            <span class="uc-nav">W</span>ild <span class="uc-nav">S</span>eries
        </span>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav nav ml-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDrop" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'langue.drop'|trans }}
                </a>

                <div class="dropdown-menu" aria-labelledby="navbarDrop">
                    <a class="dropdown-item" href="{{ path(app.request.get('_route'), ({'_locale': 'fr' })) }}">Français</a>
                    <a class="dropdown-item" href="{{ path(app.request.get('_route'), ({'_locale': 'en' })) }}">English</a>
                    <a class="dropdown-item" href="{{ path(app.request.get('_route'), ({'_locale': 'es' })) }}">Spainish</a>
                </div>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('wild_index') }}"> {{ 'programs.nav'|trans }} </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('category_index') }}"> {{ 'categories.nav'|trans }} </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('actor_index') }}"> {{ 'actors.nav'|trans }} </a>
            </li>
            {% if is_granted("ROLE_ADMIN")%}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path("program_index") }}">{{ 'program.drop'|trans }}</a>
                        <a class="dropdown-item" href="{{ path("episode_index") }}">{{ 'episode.drop'|trans }}</a>
                        <a class="dropdown-item" href="{{ path("season_index") }}">{{ 'season.drop'|trans }}</a>
                        <a class="dropdown-item" href="{{ path("actor_all") }}">{{ 'actor.drop'|trans }}</a>
                    </div>
                </li>
            {% endif %}
            <li class="nav-item">
                {% if app.user %}
                </li>
                <li class="nav-item">
                    <a href="{{ path('wild_my_profil')}}" class="nav-link">{{ 'profil'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link"> {{ 'account.logout'|trans }}<img src="{{ asset('build/images/connexion.png') }}" class="logo-nav mr-4 ml-4" alt="logo connexion"></a>
                </li>
                {% else %}
                    <a href="{{ path('app_login') }}" class="nav-link"> {{ 'account.login'|trans }}<img src="{{ asset('build/images/connexion.png') }}" class="logo-nav mr-4 ml-4" alt="logo connexion"></a>
                {% endif %}
        </ul>
    </div>
</nav>