{% extends 'base.html.twig' %}

{% block title %}Editer Saison{% endblock %}

{% block body %}
    {% include "Base/navbar.html.twig" %}
    <div class="block-main">
        <h1 class="text-center">Editer une Saison</h1>

        <div class="container">
            {{ include('season/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a href="{{ path('season_index') }}">Retour à la liste</a>

            {{ include('season/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
